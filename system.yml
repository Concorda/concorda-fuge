mesh:
  build: mesh/
  container_name: mesh

concorda:
  build: ../concorda/
  container_name: concorda

dashboard:
  build: ../concorda-dashboard/
  container_name: dashoard
  environment:
    - SERVICE_HOST=0.0.0.0
    - PORT=3050
    - LOCAL=false

client:
  build: ../vidi-dashboard/
  container_name: client
  environment:
    - SERVICE_HOST=0.0.0.0
    - WEB_PORT=3000
    - START=concorda:true
